---
- name: Atlassian Python API wrapper
  pip:
    name: atlassian-python-api
    state: present

- include_tasks: "sprint.yml"
  vars:
    logs_group: "{{ logs[group] }}"
  when: "{{ logs[group]['publish']|default(True) }}"
  with_items: '{{ logs }}'
  loop_control:
    loop_var: group
